<h1> <%= @room.name %> </h1>

This room is password-protected. Enter it so we can continue:

<%= form_tag password_room_path do %>
  <%= label_tag :password %>
  <%= password_field_tag :password %>
  <%= submit_tag 'Submit' %>
<% end %>